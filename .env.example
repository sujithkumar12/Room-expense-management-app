# Postgres connection (auto-set when you add Vercel Postgres / Neon in Vercel dashboard)
POSTGRES_URL=

# JWT secret for auth tokens (use a long random string in production)
JWT_SECRET=your-super-secret-jwt-key-change-me
